import { StyleSheet } from "react-native";

export default StyleSheet.create ({
    container: {
      backgroundColor : '#61764B',
      borderRadius: 10,
      margin:10,
    },
    task:{
        fontSize: 20,
        padding:10,
        color: 'white',
    },
})